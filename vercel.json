{
  "buildCommand": "cd apps/personal-website && npm run build",
  "installCommand": "npm install --workspaces --legacy-peer-deps",
  "outputDirectory": "apps/personal-website/dist",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD .",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
