{
  "name": "analytics-backend",
  "version": "1.0.0",
  "description": "Simple analytics backend for personal website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "gcp:setup": "bash scripts/gcp_setup.sh ${GCP_PROJECT_ID:-} ${GCP_LOCATION:-us-central1} blog-recommender-sa ${GA4_DATASET:-} ${GA4_TABLE:-events_*}",
    "gcp:smoke": "bash scripts/gcp_smoke.sh"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^8.1.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
