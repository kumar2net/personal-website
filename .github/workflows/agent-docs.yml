name: agent-docs

on:
  pull_request:

jobs:
  check:
    name: Agent metadata docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: node scripts/agent-lint.mjs
