{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"],
      "env": ["GEMINI_API_KEY", "OPENAI_API_KEY"]
    },
    "dev": {
      "persistent": true
    },
    "lint": {},
    "test": {}
  }
}
